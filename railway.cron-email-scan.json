{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "startCommand": "node -e \"fetch('https://recalltrial.app/api/cron/email-scan',{method:'POST',headers:{'X-CRON-KEY':process.env.CRON_KEY}}).then(async r=>{console.log('email-scan cron status',r.status,await r.text());process.exit(r.ok?0:1)}).catch(e=>{console.error(e);process.exit(1)})\"",
    "cronSchedule": "0 * * * *",
    "restartPolicyType": "NEVER"
  }
}
